[phases.setup]
nixPkgs = ['python311','python311Packages.pip','openssl']

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[start]
cmd = "python manage.py migrate && gunicorn auth_backend.wsgi:application --bind 0.0.0.0:8000"
